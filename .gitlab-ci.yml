stages:
    # - build
    - deploy

# build:
#     stage: build
#     rules:
#         - if: '$CI_COMMIT_BRANCH =~ /^(production|test)$/'
#     image: node:18.15.0
#     script:
#         - npm i
#         - npm run build
#     tags:
#         - deploy

deploy:
    environment: $CI_COMMIT_BRANCH
    stage: deploy
    rules:
        -   if: '$CI_COMMIT_BRANCH =~ /^(production|test)$/'
    tags:
        - deploy
    before_script:
        - pwd
    script:
        - pwd
